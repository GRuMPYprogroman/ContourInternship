<Window x:Class="WpfApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.Views"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
            
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                    <Setter Property="BorderBrush" Value="Red" />
                    <Setter Property="BorderThickness" Value="2" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <TabControl>
            <TabItem Header="Module 1">
                <StackPanel>
                    <Grid Margin="0 0 0 10">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" 
                                   Grid.Column="0" 
                                   Text="DataFile"
                                   Margin="10 0 10 5"
                                   VerticalAlignment="Center"/>
                        <Button Grid.Row="0" 
                                Grid.Column="1" 
                                Content="Open" 
                                Margin="0 0 10 5"
                                Padding="8 2 8 2"
                                VerticalAlignment="Center"
                                Command="{Binding OpenFileModule1Command}"
                                CommandParameter=".dat"/>
                        <TextBox Grid.Row="0" 
                                 Grid.Column="2"
                                 Margin="0 0 50 5"
                                 MinWidth="250" 
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Text="{Binding DatFilePathModule1, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"/>
                        
                        <TextBlock Grid.Row="1" 
                                   Grid.Column="0" 
                                   Text="CsvFile"
                                   Margin="10 0 10 0"
                                   VerticalAlignment="Center"/>
                        <Button Grid.Row="1" 
                                Grid.Column="1" 
                                Content="Open" 
                                Margin="0 0 10 0"
                                Padding="8 2 8 2"
                                VerticalAlignment="Center"
                                Command="{Binding OpenFileModule1Command}"
                                CommandParameter=".csv"/>
                        <TextBox Grid.Row="1" 
                                 Grid.Column="2"
                                 Margin="0 0 50 0"
                                 MinWidth="250"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Text="{Binding CsvFilePathModule1, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"/>
                    </Grid>
                    
                    <Button Content="Start Process"
                            HorizontalAlignment="Center"
                            Padding="8 2 8 2"
                            Margin="0 0 0 10"
                            Command="{Binding StartProcessModule1Command}"/>
                    
                    <ProgressBar Minimum="0"
                                 Maximum="100"
                                 Value="{Binding GaugePercent}"
                                 Height="15" 
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Stretch"/>
                </StackPanel>
            </TabItem>
            
            <TabItem Header="Module 2">
                <StackPanel>
                    <Grid Margin="0 0 0 10">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" 
                                   Grid.Column="0" 
                                   Text="DataFile"
                                   Margin="10 0 10 5"
                                   VerticalAlignment="Center"/>
                        <Button Grid.Row="0" 
                                Grid.Column="1" 
                                Content="Open" 
                                Margin="0 0 10 5"
                                Padding="8 2 8 2"
                                VerticalAlignment="Center"
                                Command="{Binding OpenFileModule2Command}"
                                CommandParameter=".dat"/>
                        <TextBox Grid.Row="0" 
                                 Grid.Column="2"
                                 Margin="0 0 50 5"
                                 MinWidth="250"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Text="{Binding DatFilePathModule2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"/>
                        
                        <TextBlock Grid.Row="1" 
                                   Grid.Column="0" 
                                   Text="CsvFile"
                                   Margin="10 0 10 0"
                                   VerticalAlignment="Center"/>
                        <Button Grid.Row="1" 
                                Grid.Column="1" 
                                Content="Open" 
                                Margin="0 0 10 0"
                                Padding="8 2 8 2"
                                VerticalAlignment="Center"
                                Command="{Binding OpenFileModule2Command}"
                                CommandParameter=".csv"/>
                        <TextBox Grid.Row="1" 
                                 Grid.Column="2"
                                 Margin="0 0 50 0"
                                 MinWidth="250"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Text="{Binding CsvFilePathModule2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"/>
                    </Grid>
                    
                    <Button Content="Start Process"
                            HorizontalAlignment="Center"
                            Padding="8 2 8 2"
                            Margin="0 0 0 10"
                            Command="{Binding StartProcessModule2Command}"/>
                    
                    <ProgressBar Minimum="0"
                                 Maximum="100"
                                 Value="{Binding GaugePercent}"
                                 Height="15"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Stretch"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
